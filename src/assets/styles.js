import OdinTheme from "./colors";

const styles = {
    container: {
        height: "100vh",
        backgroundColor:
        OdinTheme.fog,
        padding: 8,
        alignItems: "center",
        justifyContent: "center"
    }
};

export default styles;